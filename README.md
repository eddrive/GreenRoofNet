# GreenRoof

## Installation (Dataset Creation)

```sh
npm install
npx playwright install
```

## Create Dataset

Start the local server:

```sh
serve
```

Run the dataset script:

```sh
node captureMap.js
```